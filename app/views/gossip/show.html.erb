<div class="container pt-5">
    <h1>Potin <%= params[:id] %></h1>
    <p class="mt-3"></p>
    <div class="card mt-3">
        <div class="card-body">
            <h5 class="card-title"><%= Gossip.find(params[:id]).title %></h5>
            <div class="card-text"><%= Gossip.find(params[:id]).content %></div>
            <div class="card-text">crée le <%= Gossip.find(params[:id]).created_at.strftime("%d/%m/%Y") %></div>
            <div class="mt-3">
                <%= link_to Gossip.find(params[:id]).user.first_name, {controller: "user", action: "show", id: Gossip.find(params[:id]).user.id}, class: "btn btn-secondary" %>
                <%= link_to "Accueil", home_show_path, class: "btn btn-primary" %>
            </div>
        </div>
    </div>
</div>
